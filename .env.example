# Server configuration
SERVER_PORT=8080

# PostgreSQL Database configuration
# These are used by the Go services to connect
DB_HOST=db
DB_PORT=5432
DB_USER=buddy
DB_PASSWORD=password

# Database names for each service
# Note: These are passed to each service to tell it which DB to use.
# We will set these specifically for each service in docker-compose.yml
# DB_NAME=users_db

# PostgreSQL Container configuration
# These are used by the postgres container itself to initialize.
POSTGRES_USER=buddy
POSTGRES_PASSWORD=password
POSTGRES_DB=buddy_db # The default DB, our init script creates the others

# Service-to-Service Communication URLs (gRPC)
# Example for users-service connecting to srs-service
SRS_SERVICE_URL=srs-service:50051
# Production Environment Configuration for Wise Owl
# This file shows production configuration with AWS DocumentDB
# Copy this file to .env.production and update with your actual values

# Server Configuration
SERVER_PORT=8080

# AWS DocumentDB Configuration
DB_TYPE=documentdb
MONGODB_URI=mongodb://username:password@your-cluster.cluster-xyz.docdb.us-east-1.amazonaws.com:27017/?ssl=true&replicaSet=rs0&readPreference=secondaryPreferred

# Service URLs (for inter-service communication in production)
USERS_SERVICE_URL=users-service:50051
CONTENT_SERVICE_URL=content-service:50052

# Auth0 Configuration (production values)
AUTH0_DOMAIN=your-production-auth0-domain.auth0.com
AUTH0_AUDIENCE=your-production-auth0-audience

# Production Environment Flag
NODE_ENV=production

# SSL Configuration for DocumentDB
SSL_CERT_PATH=/etc/ssl/certs/rds-ca-2019-root.pem

# AWS Configuration (if using IAM roles, these can be omitted)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
